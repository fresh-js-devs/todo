import React, { useState } from 'react';
import styled from "@emotion/styled";

const BodyOfMine = styled.div({
    width: "100%",
    backgroundColor: "red",
    marginTop: "10px",
    display: "flex",
  
    flexDirection: "row",
    
})
export default BodyOfMine;