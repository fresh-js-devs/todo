import React, { useState } from 'react';
import styled from "@emotion/styled";

const WorkTasks = styled.div({
    width: "50%",
    height: "100%",
    backgroundColor: "white",
    margin: "10px",
    border: "1px solid",
    display: "flex",
    flexDirection: "column",
    })
export default WorkTasks;