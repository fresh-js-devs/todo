import React, { useState } from 'react';
import styled from "@emotion/styled";

const HeaderOfMine = styled.div({
    width: "100%",
    height: "100px",
    backgroundColor: "yellow",
    borderBottom: "2px solid black",
    display: "flex",
}    
)
export default HeaderOfMine;