import React, { useState } from 'react';
import styled from "@emotion/styled";

const PersonalTasks = styled.div({
    width: "50%",
    
    backgroundColor: "green",
    margin: "10px",
    border: "1px solid",
    display: "flex",
    flexDirection: "column",
    
})
export default PersonalTasks;