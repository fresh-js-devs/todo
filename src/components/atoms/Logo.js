import styled from "@emotion/styled";

const Logo = styled.div({
    fontSize: "50px",
    fontFamily: "Impact, Charcoal, sans-serif",
    marginTop: "15px",
    marginLeft: "20px",
})

export default Logo;