import styled from "@emotion/styled";

const Input = styled.input({
  borderRadius: "20px",
  border: "none",
  padding: "15px",
  boxShadow: "0px 2px 6px rgba(0, 0, 0, 0.16), 0 2px 6px rgba(0, 0, 0, 0.32)"
});

export default Input;
